# 書類作成機能 開発ログ

## 概要
介護管理ツールにおける書類作成機能の開発・更新履歴を記録します。

## 開発開始日
2025-11-18

---

## 更新履歴

### 2025-11-29

#### 介護予防サービス計画作成・介護予防ケアマネジメント依頼（変更）届出書 - Excel出力完成
**フェーズ**: Excel座標設定・データ連携
**ステータス**: 完成

**概要**:
介護予防サービス計画作成・介護予防ケアマネジメント依頼（変更）届出書のExcel出力機能を完成させました。受託事業者（居宅介護支援事業所）のセクション、利用者住所、データクリア処理を実装。

**変更ファイル**:
- `static/config/excel_coordinates.json` - 受託事業者および利用者住所の座標追加
- `clients/views.py` - Excel出力ロジック追加、データクリア処理追加
- `templates/clients/document_create_preventive.html` - データクリア処理追加

**実装内容**:

1. **介護予防支援を受託する居宅介護支援事業者のExcel座標追加**
   ```json
   "jutaku_provider_name": "A18",
   "jutaku_provider_postal_code": "AD17",
   "jutaku_provider_address": "L18",
   "jutaku_provider_phone": "U19",
   "jutaku_provider_staff_name": "U20",
   "jutaku_provider_office_number_1": "A20",
   ...
   "jutaku_provider_office_number_10": "J20"
   ```

2. **利用者住所のExcel座標追加（介護予防版）**
   ```json
   "client_address": "J35"
   ```

3. **Excel出力ロジックの追加（views.py 1016-1052行目）**
   - `jutaku_provider`が選択されている場合のみ書き込み
   - 事業所名はユーザーの所属事業所（`request.user.profile.home_care_office.name`）から取得
   - 事業所番号、郵便番号、住所、電話番号、担当者氏名を出力
   - 事業所番号は10桁に分割して各セルに数値として書き込み

4. **利用者住所の出力ロジック（views.py 979-981行目）**
   - フォームで編集された住所があればそれを使用
   - なければクライアントマスタの住所を使用

5. **受託事業者データのクリア処理**
   - フロントエンド（document_create_preventive.html 886-891行目）:
     - `updateJutakuSection`関数で、安濃津ろまん選択時にjutaku関連フィールドをクリア
   - バックエンド（views.py 693-700行目）:
     - `provider_category`が'anoutsu'の場合、jutaku関連データを空文字列でクリア
     - 内部的にも受託事業者のデータが残らないように保証

**技術的なポイント**:
- 受託事業者セクションの条件分岐（jutaku_providerの有無で出力判定）
- preventive_provider、kyotaku_provider、jutaku_providerの3セクションで統一されたロジック
- データクリア処理をフロントエンドとバックエンドの両方で実装し、データ整合性を確保

**完成した書類**:
✅ **介護予防サービス計画作成・介護予防ケアマネジメント依頼（変更）届出書**
- 入力フォーム完成
- マスタデータ編集機能実装済み
- Excel出力機能完成
- すべての必須項目のマッピング完了

---

#### マスタデータ編集機能の実装
**フェーズ**: 機能追加
**ステータス**: 完成

**概要**:
書類作成画面で地域包括支援センターおよび居宅介護支援事業所のマスタデータを直接編集できる機能を追加。ドロワーUIで編集し、保存すると即座にデータベースが更新され、フォームに最新データが反映されます。

**変更ファイル**:
- `clients/api_views.py` - マスタデータ更新API追加（update_center_master, update_office_master）
- `clients/urls.py` - APIエンドポイント追加
- `templates/clients/document_create_preventive.html` - ドロワーUI、JavaScript実装
- `templates/clients/document_create_kyotaku.html` - ドロワーUI、JavaScript実装
- `clients/views.py` - 既存書類編集時の最新マスタデータ反映処理追加

**実装内容**:

1. **マスタ編集ドロワーUI**
   - 地域包括支援センター編集ボタン（介護予防版）
   - 居宅介護支援事業所編集ボタン（介護予防版・居宅版）
   - ドロワーで事業所番号、郵便番号、住所、電話番号を編集可能
   - 事業所名は変更不可（readonly）

2. **保存して反映機能**
   - `saveCenterData()` / `saveOfficeData()` 関数でAPIを呼び出し
   - データベース更新とフォームへの反映を同時実行
   - グローバル変数（currentEditData, supportCentersFromDB）も更新

3. **APIエンドポイント**
   ```python
   # 地域包括支援センター更新
   POST /clients/center/<int:pk>/update-master/

   # 居宅介護支援事業所更新
   POST /clients/office/<int:pk>/update-master/
   ```

4. **ステータスアイコンのリアルタイム更新**
   - マスタデータ保存後、ビックリマーク/チェックマークが即座に更新
   - `window.updateHoukatsuStatusIcon()` / `window.updateJutakuStatusIcon()` / `window.updateKyotakuStatusIcon()` をグローバルスコープに登録
   - ページリロード不要

5. **既存書類編集時の最新マスタデータ反映**
   - 介護予防版：地域包括支援センター情報を最新に更新（513-525行目）
   - 介護予防版：居宅介護支援事業所情報を最新に更新（528-569行目）
   - 居宅版：居宅介護支援事業所情報を最新に更新（572-577行目）
   - `HomeCareSupportOffice.objects.get(pk=...)` でデータベースから最新値を取得

6. **介護予防版の受託事業所情報の修正**
   - ハードコードされていた古い値（2472400072, 514-0824等）を削除
   - `anoutsuData` をログインユーザーの所属事業所から動的に取得
   ```javascript
   const anoutsuData = {
       name: '{% if user.profile.home_care_office %}{{ user.profile.home_care_office.name }}{% endif %}',
       office_number: '{% if user.profile.home_care_office %}{{ user.profile.home_care_office.office_number }}{% endif %}',
       // ...
   };
   ```

**技術的なポイント**:
- Fetch APIでCSRFトークンを含むPOSTリクエストを送信
- try-catchでエラーハンドリングを実装
- 関数の存在チェック（`typeof updateHoukatsuStatusIcon === 'function'`）で安全に呼び出し
- ドロワーHTMLをメインcontentブロック内に配置（ベーステンプレートにdrawer_sectionブロックがないため）

**メリット**:
- ✅ 書類作成画面から直接マスタデータを編集可能
- ✅ 変更が即座にデータベースに反映
- ✅ 既存書類を編集する際、常に最新のマスタデータが表示される
- ✅ ステータスアイコンがリアルタイムで更新され、入力状態が一目で分かる
- ✅ ユーザビリティの大幅な向上

---

### 2025-11-25

#### Clientモデルの主キー変更（insurance_number → id）
**フェーズ**: データベース構造変更
**ステータス**: 完成

**概要**:
介護保険被保険者番号（insurance_number）を主キーから通常のユニークフィールドに変更し、自動採番ID（id）を主キーとして使用するようにモデル構造を改善。これにより、介護保険被保険者番号を間違えて登録した場合でも、編集画面で修正可能になりました。

**変更ファイル**:
- `clients/models.py` - Clientモデルの主キーを変更
- `assessments/models.py` - 外部キー参照を修正
- `clients/views.py` - 介護保険被保険者番号の更新ロジックを有効化
- `templates/clients/document_create_base.html` - 介護保険被保険者番号を変更検出対象に追加
- `restore_data.py` - データ移行スクリプト（新規作成）

**実装内容**:

1. **モデル変更**
   ```python
   # 変更前
   insurance_number = models.CharField('被保険者番号', max_length=20, unique=True, primary_key=True)

   # 変更後
   # id = models.AutoField(primary_key=True)  # Djangoが自動追加
   insurance_number = models.CharField('被保険者番号', max_length=20, unique=True)
   ```

2. **外部キー参照の修正**
   - Assessmentモデル: `to_field='insurance_number'` を削除
   - 自動的に新しい主キー（id）を参照

3. **データ移行手順**
   - 既存データをJSON形式でエクスポート
   - データベースとマイグレーションファイルを削除
   - 新しいスキーマでマイグレーション実行
   - カスタムスクリプトでデータ復元（insurance_number → id マッピング）

4. **データ復元スクリプト（restore_data.py）**
   - Client 23件を復元（自動採番IDで）
   - Assessment 25件を復元（外部キー参照を新IDに変換）
   - 関連データ（LimitCalculation、DocumentCreationHistory等）の外部キーを変換

5. **介護保険被保険者番号の編集機能**
   - 書類作成画面で介護保険被保険者番号を変更した場合、データベースも更新可能に
   - 変更検出モーダルに表示され、ユーザーが更新を選択可能
   - 主キーではなくなったため、外部キー制約エラーが発生しない

**技術的なポイント**:
- SQLiteでは主キーを直接変更できないため、データベースを再作成
- 既存データを保持するため、カスタムスクリプトでデータ移行
- insurance_numberからidへのマッピングテーブルを作成し、外部キー参照を適切に変換
- UTF-8/Shift_JISエンコーディングの違いに対応

**メリット**:
- ✅ 介護保険被保険者番号を編集画面で修正可能
- ✅ 間違えて登録した場合でも、データを失わずに修正できる
- ✅ 標準的なDjangoの設計パターンに準拠
- ✅ 将来的な拡張が容易

---

### 2025-11-24

#### 利用者基本情報のデータベース更新機能
**フェーズ**: 機能追加
**ステータス**: 完成

**概要**:
書類作成画面で利用者基本情報（データベースから自動反映される項目）を変更した場合、保存時にデータベース本体を更新するかどうかを確認するポップアップを表示し、ユーザーの選択に応じてデータベースを更新する機能を追加。

**変更ファイル**:
- `templates/clients/document_create_base.html` - 変更検出用hidden項目、確認モーダル、JavaScript追加
- `clients/views.py` - データベース更新処理の追加

**実装内容**:

1. **変更検出対象フィールド**
   - 氏名（client_name）
   - ふりがな（client_furigana）
   - 生年月日（client_birth_date）
   - 住所（client_address）
   - 介護保険被保険者番号（client_insurance_number）

2. **変更検出の仕組み**
   - テンプレートにhidden項目で元のデータベース値を保持
   ```html
   <input type="hidden" id="original_client_name" value="{{ client.name }}">
   <input type="hidden" id="original_client_insurance_number" value="{{ client.insurance_number }}">
   ```
   - JavaScript `detectClientChanges()` 関数で現在のフォーム値と元の値を比較
   - 差分がある場合のみ確認モーダルを表示

3. **確認モーダル**
   - 変更されたフィールド名をリスト表示
   - 「更新する」ボタン：データベースを更新して保存
   - 「更新しない」ボタン：データベースは更新せず書類のみ保存
   - 「キャンセル」ボタン：保存をキャンセル

4. **サーバー側処理（views.py）**
   - `update_client_db` POSTパラメータで更新有無を判定
   - 直接 `Client.objects.filter(pk=client.pk).update(**update_data)` でDB更新
   - 更新成功時にメッセージ表示：「利用者情報を更新しました: 〇〇, △△」

**技術的なポイント**:
- フォーム保存時に `detectClientChanges()` を呼び出し、変更があればモーダル表示
- モーダルの選択結果を `updateClientDbField` hidden項目にセット
- 日本語文字列比較の問題を回避するため、update_data辞書を直接構築

---

#### バリデーション改善（フォーカス機能・ステータスアイコン修正）
**フェーズ**: バグ修正・UI改善
**ステータス**: 完成

**変更ファイル**:
- `templates/clients/document_create_base.html` - validateDocumentSpecific()呼び出し追加
- `templates/clients/document_create_preventive.html` - ステータスアイコン判定修正
- `templates/clients/document_create_kyotaku.html` - openAccordionAndFocus()関数追加

**改善内容**:

1. **validateDocumentSpecific()の統合**
   - ベーステンプレートのvalidateForm()から子テンプレートのvalidateDocumentSpecific()を呼び出し
   ```javascript
   function validateForm() {
       if (!validateFormBase()) return false;
       if (typeof validateDocumentSpecific === 'function') {
           if (!validateDocumentSpecific()) return false;
       }
       return true;
   }
   ```

2. **バリデーションエラー時のフォーカス機能（居宅版）**
   - アコーディオンが閉じている場合は自動で開く
   - エラーのある入力項目にフォーカス
   - スムーズスクロールで画面中央に表示
   ```javascript
   const openAccordionAndFocus = function(element) {
       const collapse = document.getElementById('kyotakuDetailCollapse');
       if (collapse && !collapse.classList.contains('show')) {
           new bootstrap.Collapse(collapse, { show: true });
           setTimeout(function() {
               element.focus();
               element.scrollIntoView({ behavior: 'smooth', block: 'center' });
           }, 400);
       }
   };
   ```

3. **ステータスアイコン修正（介護予防版）**
   - 安濃津ろまん選択時のアイコン判定を修正
   - 以前：担当者氏名のみチェック
   - 修正後：事業所番号、郵便番号、住所、電話番号、担当者氏名すべてをチェック
   ```javascript
   if (category === 'anoutsu') {
       const officeNumber = document.getElementById('houkatsu_office_number').value.trim();
       const postalCode = document.getElementById('houkatsu_postal_code').value.trim();
       // ... 全フィールドをチェック
       isComplete = officeNumber !== '' && postalCode !== '' && address !== '' && phone !== '' && staffName !== '';
   }
   ```

---

#### 介護予防版 - 受託事業者のデフォルト選択変更
**フェーズ**: UI改善
**ステータス**: 完成

**変更ファイル**:
- `templates/clients/document_create_preventive.html`

**変更内容**:
- 「介護予防支援を受託する居宅介護支援事業者」のデフォルト選択を変更
- 以前：「選択してください」が初期選択
- 変更後：「居宅介護支援事業所　安濃津ろまん」が初期選択

```html
<!-- 変更前 -->
<select name="jutaku_provider" id="jutaku_provider" class="form-select">
    <option value="">選択してください</option>
    <option value="anoutsu_roman">居宅介護支援事業所　安濃津ろまん</option>
</select>

<!-- 変更後 -->
<select name="jutaku_provider" id="jutaku_provider" class="form-select">
    <option value="anoutsu_roman" selected>居宅介護支援事業所　安濃津ろまん</option>
</select>
```

---

#### 居宅サービス計画作成依頼（変更）届出書 - 事業者選択機能の追加
**フェーズ**: 機能追加
**ステータス**: 完成

**変更内容**:
介護予防版と同様の事業者選択機能を居宅サービス計画作成依頼（変更）届出書にも追加。

**変更ファイル**:
- `templates/clients/document_create_kyotaku.html` - 事業者選択UIの追加
- `templates/clients/document_create_base.html` - validateForm()の修正
- `clients/views.py` - form_dataに居宅版事業者情報を追加
- `static/config/excel_coordinates.json` - 居宅版事業者座標の追加

**実装内容**:

1. **事業者選択プルダウン**
   - 「居宅介護支援事業所　安濃津ろまん」のみ選択可能（介護予防版と異なり地域包括支援センターは選択不可）

2. **事業所情報セクション（アコーディオン）**
   - 事業所番号: 2472400072（デフォルト）
   - 郵便番号: 514-0824（デフォルト）
   - 住所: 津市神戸154-9（デフォルト）
   - 電話番号: 059-253-6599（デフォルト）
   - 担当者氏名: ログインユーザー名（デフォルト）、**必須項目**

3. **ステータスアイコン**
   - 必須項目がすべて入力済み: ✓ 緑チェックマーク (`fa-check-circle`, rgba(25, 135, 84, 0.5))
   - 必須項目に未入力あり: ! 赤ビックリマーク (`fa-circle-exclamation`, rgba(220, 53, 69, 0.5))

4. **バリデーション機能**
   - `validateKyotakuProvider()` 関数で事業所番号、郵便番号、住所、電話番号、担当者氏名を必須チェック
   - `validateDocumentSpecific()` 関数をベーステンプレートから呼び出し
   - バリデーションエラー時にアコーディオンを自動で開く

5. **データ保存機能**
   - 事業所情報の変更を保存時に履歴（form_data）に保存
   - 編集時に保存された値を復元

**Excel座標設定**:
```json
"kyotaku_provider_name": "A13",
"kyotaku_provider_postal_code": "AD12",
"kyotaku_provider_address": "L13",
"kyotaku_provider_phone": "U14",
"kyotaku_provider_staff_name": "U15",
"kyotaku_provider_office_number_1": "A15",
"kyotaku_provider_office_number_2": "B15",
...
"kyotaku_provider_office_number_10": "J15"
```

---

#### 介護保険被保険者番号のExcelマッピング修正
**フェーズ**: バグ修正
**ステータス**: 完成

**問題**:
- 介護保険被保険者番号が正しいセルに出力されない
- フォームで修正した介護保険番号がExcel出力に反映されない

**原因**:
- Excel座標が連続したセル（P5, Q5, R5...）として設定されていたが、実際は1セル飛ばし（P5, R5, T5...）
- Excel出力時に`client.insurance_number`を直接使用していたため、フォームでの修正が反映されなかった

**変更ファイル**:
- `static/config/excel_coordinates.json` - 座標設定の修正
- `clients/views.py` - form_dataから介護保険番号を取得するように修正

**修正内容**:
```json
// 修正後の座標（1セル飛ばし）
"insurance_number_1": "P5",
"insurance_number_2": "R5",
"insurance_number_3": "T5",
"insurance_number_4": "V5",
"insurance_number_5": "X5",
"insurance_number_6": "Z5",
"insurance_number_7": "AB5",
"insurance_number_8": "AD5",
"insurance_number_9": "AF5",
"insurance_number_10": "AH5"
```

```python
# 修正後: form_dataから取得（フォームでの修正を反映）
insurance_number = form_data.get('client_insurance_number') or client.insurance_number
```

---

#### 生年月日が反映されない問題の修正
**フェーズ**: バグ修正
**ステータス**: 完成

**問題**:
介護予防サービス計画作成・介護予防ケアマネジメント依頼（変更）届出書及び居宅サービス計画作成依頼（変更）届出書で、利用者の生年月日が反映されなくなった。新規作成時・編集時の両方で発生。

**原因**:
テンプレートのフィルターチェーンが問題だった。
```html
value="{{ initial_data.client_birth_date|default:client.birth_date|date:'Y-m-d' }}"
```
- `initial_data.client_birth_date` が文字列（例: "2020-01-01"）の場合、`default` フィルターはこの値をそのまま返す
- `|date:'Y-m-d'` フィルターが文字列に適用される
- `date` フィルターは日付オブジェクトを期待しているため、文字列に対しては空文字列を返す

**変更ファイル**:
- `templates/clients/document_create_base.html` - 介護予防版のベーステンプレート
- `templates/clients/document_create.html` - 居宅版のテンプレート

**修正内容**:
```html
<!-- 修正前 -->
value="{{ initial_data.client_birth_date|default:client.birth_date|date:'Y-m-d' }}"

<!-- 修正後 -->
value="{% if initial_data.client_birth_date %}{{ initial_data.client_birth_date }}{% else %}{{ client.birth_date|date:'Y-m-d' }}{% endif %}"
```

条件分岐を使用して、`initial_data.client_birth_date` がある場合はそれを直接使用し、ない場合のみ `client.birth_date` に `date` フィルターを適用するように変更。

---

#### Excel出力の外部リンクエラー修正
**フェーズ**: バグ修正
**ステータス**: 完成

**問題**:
介護予防サービス計画作成・介護予防ケアマネジメント依頼（変更）届出書のExcel出力時に「一部の内容に問題が見つかりました」という警告が表示されていた。

**原因**:
テンプレートファイル `kyotaku_preventive_service_plan_request.xlsx` に外部リンク（他のExcelファイルへの参照）が含まれていた。openpyxlで保存時にこの外部リンク情報が壊れていた。

**変更ファイル**:
- `clients/views.py` - Excel読み込み時に外部リンクを削除する処理を追加

**修正内容**:
```python
# 外部リンクを削除（保存時のエラー回避）
if hasattr(workbook, '_external_links'):
    workbook._external_links = []
```

---

#### 和暦変換ロジックの修正
**フェーズ**: バグ修正
**ステータス**: 完成

**問題**:
1. Excel出力時に1989年1月1日〜7日が「平成01年」と表示されていた（正しくは「昭和64年」）
2. ブラウザの日付ピッカーのカレンダー内で和暦が正しく表示されない

**原因**:
1. Excel出力の `to_wareki` 関数が年だけで元号を判定していた（月日を考慮していなかった）
2. ブラウザ標準のHTML5日付ピッカーの和暦表示はブラウザ/OS側の制御

**変更ファイル**:
- `clients/views.py` - `to_wareki` 関数を修正
- `clients/templatetags/custom_filters.py` - テンプレートフィルターは元々正しかった（参考）
- `templates/base.html` - 日付入力欄の下に正しい和暦を表示するJavaScriptを追加

**修正内容**:

1. **Excel出力の和暦変換（views.py）**:
```python
def to_wareki(date):
    from datetime import date as date_class
    # 令和: 2019年5月1日〜
    if date >= date_class(2019, 5, 1):
        era = "令和"
    # 平成: 1989年1月8日〜2019年4月30日
    elif date >= date_class(1989, 1, 8):
        era = "平成"
    # 昭和: 1926年12月25日〜1989年1月7日
    elif date >= date_class(1926, 12, 25):
        era = "昭和"
    # 大正・明治にも対応

    # 元年表示に対応
    if wareki_year == 1:
        return f"{era}元年{month:02d}月{day:02d}日"
```

2. **ブラウザ日付入力の和暦表示（base.html）**:
- 全ての `<input type="date">` に対して和暦表示ラベルを自動追加
- 日付変更時にリアルタイムで更新
- 正しい元号境界で判定

**正しい和暦の境界**:
| 元号 | 開始日 | 終了日 |
|------|--------|--------|
| 明治 | 1868年1月25日 | 1912年7月29日 |
| 大正 | 1912年7月30日 | 1926年12月24日 |
| 昭和 | 1926年12月25日 | 1989年1月7日 |
| 平成 | 1989年1月8日 | 2019年4月30日 |
| 令和 | 2019年5月1日 | 現在 |

---

#### .xlsx/.xlsm ファイル形式の自動判別
**フェーズ**: 機能改善
**ステータス**: 完成

**問題**:
.xlsxテンプレートを使用しているのに、ダウンロード時に.xlsmとして出力されていた。

**変更ファイル**:
- `clients/views.py` - テンプレートファイルの拡張子を自動判別

**修正内容**:
```python
# テンプレートファイルのパス（.xlsxと.xlsmの両方をサポート）
template_path_xlsx = os.path.join(settings.BASE_DIR, 'templates', 'forms', f'{document_type}.xlsx')
template_path_xlsm = os.path.join(settings.BASE_DIR, 'templates', 'forms', f'{document_type}.xlsm')

if os.path.exists(template_path_xlsm):
    template_path = template_path_xlsm
    is_macro_enabled = True
elif os.path.exists(template_path_xlsx):
    template_path = template_path_xlsx
    is_macro_enabled = False

# 出力時に正しい拡張子とContent-Typeを使用
file_ext = '.xlsm' if is_macro_enabled else '.xlsx'
```

---

#### 介護予防サービス計画作成依頼届出書 - アコーディオンUI改善
**フェーズ**: UI改善
**ステータス**: 完成

**変更ファイル**:
- `templates/clients/document_create.html` - アコーディオンのロジック整理、ステータスアイコン追加

**改善内容**:

1. **アコーディオンの開閉ロジック統一**
   - 基本的にアコーディオンは閉じた状態を維持
   - カテゴリ変更時（津市・津市以外選択）にもアコーディオンを閉じる
   - `openHoukatsuAccordion()` / `closeHoukatsuAccordion()` 関数でクラス操作による制御

2. **事業所情報のステータスアイコン追加**
   - アコーディオンタイトル左側にステータスアイコンを表示
   - 必須項目がすべて入力済み: ✓ 緑チェックマーク (`fa-check-circle`, rgba(25, 135, 84, 0.5))
   - 必須項目に未入力あり: ! 赤ビックリマーク (`fa-circle-exclamation`, rgba(220, 53, 69, 0.5))
   - 入力時にリアルタイムでアイコンが更新される

3. **必須項目の判定基準**
   - 安濃津ろまん: 担当者氏名
   - 県外: 事業所名、事業所番号、郵便番号、住所、電話番号
   - 地域包括支援センター: 事業所番号、郵便番号、住所、電話番号

4. **受託事業者セクションの改善**
   - 同様のステータスアイコンを追加
   - 担当者氏名の入力状態でアイコンが変化
   - 初期選択で「安濃津ろまん」が選択された状態

**技術的なポイント**:
- `updateHoukatsuStatusIcon()` 関数でアイコン更新
- `updateJutakuStatusIcon()` 関数で受託事業者のアイコン更新
- `input`イベントリスナーでリアルタイム更新
- プルダウンの値を直接取得して判定

---

#### カラー参照ページ - パターン26〜34追加
**ステータス**: 完成

**変更ファイル**:
- `templates/clients/color_reference.html` - アイコン用パステルカラーパターン追加

**追加内容**:
パターン26〜34（アイコン用パステルカラー: 透明背景・枠線なし）

| 番号 | 背景色（rgba, 0.5透明度） |
|------|--------------------------|
| 26 | rgba(153, 153, 153, 0.5) - グレー |
| 27 | rgba(13, 110, 253, 0.5) - ブルー |
| 28 | rgba(25, 135, 84, 0.5) - グリーン |
| 29 | rgba(253, 126, 20, 0.5) - オレンジ |
| 30 | rgba(111, 66, 193, 0.5) - パープル |
| 31 | rgba(220, 53, 69, 0.5) - レッド |
| 32 | rgba(214, 51, 132, 0.5) - ピンク |
| 33 | rgba(255, 193, 7, 0.5) - イエロー |
| 34 | rgba(210, 180, 140, 0.5) - タン |

---

### 2025-11-23

#### 介護予防サービス計画作成依頼届出書 - 事業者選択機能の改善
**フェーズ**: 機能改善
**ステータス**: 完成

**変更ファイル**:
- `clients/views.py` - RegionalSupportCenterデータのJSON化、provider_category保存対応
- `templates/clients/document_create.html` - 事業者選択UIの全面改修

**改善内容**:

1. **地域包括支援センターのDB連携**
   - ハードコードされていたセンターデータをRegionalSupportCenterモデルから取得
   - views.pyでJSONに変換してテンプレートに渡す
   - 新規センター追加時に自動で選択肢に反映

2. **事業者種別の保存・復元機能**
   - `provider_category`をform_dataに追加
   - 保存後に再編集で開いた際に選択状態を維持

3. **安濃津ろまん選択時の動作**
   - 第2プルダウンを非表示に
   - 詳細セクション（グレー背景）に事業所情報を自動入力
   - 事業所番号: 2472400072、〒514-0824、津市神戸154-9、059-253-6599

4. **必須項目のバリデーション**
   - 事業所番号、郵便番号、住所、電話番号を必須に
   - 担当者氏名は空白でも可
   - 保存時にJavaScriptでチェック

5. **県外オプションの追加**
   - 事業者種別に「地域包括支援センター（県外）」を追加
   - 県外選択時は事業所名を手入力
   - 詳細情報（事業所番号、住所等）も全て手入力

6. **県外選択時のレイアウト改善**
   - 事業所名入力欄をグレー背景セクション内に移動
   - レイアウト: 1行目「事業所名 | 事業所番号」、2行目「所在地」、3行目「電話番号 | 担当者氏名」
   - 県外以外のカテゴリでは事業所名欄は非表示

7. **県外切り替え時のクリア機能**
   - 他のカテゴリから県外に切り替えた時、全フィールドをクリア
   - 県外として保存したデータを再編集する場合は保存値を維持
   - `isInitialLoad`パラメータで初期ロードとユーザー操作を区別

**技術的なポイント**:
- `showDetailSection(category, providerName, isInitialLoad)` 関数でセクション表示を制御
- 初期ロード時は`isInitialLoad=true`で保存データを維持
- ユーザー操作時は`isInitialLoad=false`でフィールドをクリア
- `kengai_provider_name_section`で県外時のみ事業所名入力欄を表示

---

#### 介護予防サービス計画作成・介護予防ケアマネジメント依頼（変更）届出書 - 入力フォーム作成
**フェーズ**: 入力フォーム作成
**ステータス**: 完成

**変更ファイル**:
- `clients/models.py` - DOCUMENT_TYPE_CHOICESに'kyotaku_preventive_service_plan_request'を追加
- `clients/views.py` - document_typesディクショナリに介護予防版を追加、form_dataにpreventive_support_providerを追加
- `templates/clients/client_list.html` - selectDocument関数にマッピングを追加、ステータスを「利用可能」に変更
- `templates/clients/document_create.html` - 介護予防版専用の事業者選択欄を追加、タイトルを動的表示に変更

**備考**:
- 居宅サービス計画作成依頼（変更）届出書と同じフォームをベースに使用
- 介護予防版専用項目「介護予防サービス計画の作成を依頼（変更）する介護予防支援事業者」を追加
- **2段階プルダウン方式で実装**:
  1. 第1プルダウン「事業者種別を選択」:
     - 安濃津ろまん
     - 地域包括支援センター（津市）
     - 地域包括支援センター（津市以外）
  2. 第2プルダウン（種別選択後に表示）:
     - 安濃津ろまん選択時: 居宅介護支援事業　安濃津ろまん
     - 津市選択時: 津市地域包括支援センター（11施設）
     - 津市以外選択時: その他の地域包括支援センター
- **津市地域包括支援センター一覧**（プリセットデータ登録済み）:
  - 津市地域包括支援センター（2400500019）
  - 津一志地域包括支援センター（2400500027）
  - 津北部東地域包括支援センター（2400500035）
  - 津北部西地域包括支援センター（2400500043）
  - 津中部西地域包括支援センター（2400500050、514-0826、津市野田2059、059-237-2018）
  - 津中部東地域包括支援センター（2400500092、津市津興2947、059-213-8115）
  - 津中央地域包括支援センター（2400500118、津市東丸之内4-21、059-253-5225）
  - 津中部中地域包括支援センター（2400500100、津市渋見町554-69、059-271-6535）
  - 津中部北地域包括支援センター（2400500084、津市島崎町97-1、059-213-3181）
  - 津中部南地域包括支援センター（2400500068、津市高茶屋小森町4152、059-238-6511）
  - 津久居地域包括支援センター（2400500076）
- 地域包括支援センター選択時に詳細入力欄を表示（グレー背景）
  - 事業所番号、郵便番号、所在地、電話番号、担当者氏名（空白でも可）
  - 津市の各センター選択時はプリセット値を自動入力
  - 「その他」選択時は手入力
- タイトルはdocument_nameから動的に表示

#### 一覧画面のクライアントサイドフィルタリング実装
**ステータス**: 完成

**概要**:
各一覧画面のフィルタリング機能を、サーバーサイドからクライアントサイド（JavaScript）に変更し、ページリロードなしで即座にフィルタリング結果を表示できるように改善。

**変更ファイル**:

1. **templates/base.html**
   - `{% block extra_js %}{% endblock %}` を追加（bodyタグ終了直前）
   - 各ページのJavaScriptが読み込まれるように修正

2. **templates/clients/support_center_list.html（地域包括支援センター一覧）**
   - formタグをdivタグに変更（サーバー送信を防止）
   - アコーディオンアイテムにdata属性を追加（data-area, data-office-number, data-address）
   - クライアントサイドフィルタリング用JavaScriptを実装
   - クリアボタンの動作を修正

3. **assessments/views.py**
   - `assessment_list`ビューからサーバーサイドフィルタリングを削除
   - 全データを返し、フィルタリングはクライアントサイドで実行

4. **templates/assessments/assessment_list.html（アセスメント一覧）**
   - formタグをdivタグに変更
   - テーブル行にdata属性を追加（data-name, data-furigana, data-care-level, data-assessor-id）
   - クライアントサイドフィルタリング用JavaScriptを実装
   - 件数バッジをリアルタイム更新

5. **clients/views.py**
   - `client_list`ビューからサーバーサイドフィルタリングを削除
   - 全データを返し、フィルタリングはクライアントサイドで実行

6. **templates/clients/client_list.html（利用者一覧）**
   - formタグをdivタグに変更
   - `allClientsData`配列でクライアントデータをJavaScriptに渡す
   - `created_by_id`を追加してケアマネフィルターに対応
   - クライアントサイドフィルタリング用JavaScriptを実装
   - テーブル動的更新機能を実装

**技術的なポイント**:
- フォーム（form）をdiv要素に変更することで、入力変更時のサーバー送信を防止
- data属性を使用して、各行に検索対象データを埋め込み
- JavaScriptでリアルタイムフィルタリングを実行
- 件数バッジをフィルタリング結果に応じて動的更新
- 日本語IME入力対応（compositionstart/compositionend イベント）

**日本語入力（IME）対応**:
- 日本語入力中に検索が実行されてしまう問題を修正
- `compositionstart`/`compositionend`イベントを使用してIME状態を追跡
- 変換確定後（Enter）に検索を実行するように変更

**メリット**:
- ページリロードなしで即座にフィルタリング結果を表示
- サーバー負荷の軽減
- ユーザー体験の向上
- 統一されたフィルタリングUI/UX

---

### 2025-11-18

#### 初期セットアップ
- 書類作成.mdファイルを作成
- 開発ログの記録を開始

#### 書類一覧の更新（15:40）
利用者一覧ページの書類選択モーダルを実際の書類テンプレートに合わせて更新しました。

**変更内容:**
- `templates/clients/client_list.html` の書類選択モーダルを更新
- 仮の書類名を削除し、実際に使用する書類名に変更
- カテゴリを3つに再編成：
  1. 介護保険関係（6種類）
  2. ケアプラン・サービス関係（2種類）
  3. 基本情報・医療連携（3種類）

**追加した書類:**
1. 居宅サービス事業所の選択に関する説明に係る確認書
2. サービス担当者会議開催のご案内
3. 介護サービス計画作成に係る資料提供申請書
4. フェースシート
5. 入院時情報提供書
6. 介護予防サービス計画作成・介護予防ケアマネジメント依頼（変更）届出書
7. 介護保険 要介護・要支援 [認定区分変更]申請書
8. 介護保険　要介護・要支援　（更新）認定申請書
9. 介護保険関係・再交付申請書
10. 医療・介護連携シート

**対応するテンプレートファイル:**
- care_provider_selection_confirm.xlsx
- care_service_meeting_notice.xlsx
- CarePlan_Info_Request.xls
- Face Sheet.xlsx
- hospital_admission_info_form.xlsx
- kyotaku_preventive_service_plan_request.xlsx
- LTC_Certification_Change_R4-08.xlsx
- LTC_Certification_Renewal_R4-08.xlsx
- LTC_Reissue_Application.xlsx
- medical_care_coordination_sheet.xlsx

#### 現状確認（15:36）
既存の実装状況を確認しました。

##### 実装済みの機能
1. **データモデル**
   - `DocumentCreationHistory` モデル（clients/models.py:382）
   - 書類種別: 居宅サービス計画作成依頼書、ケアプラン、アセスメント、その他
   - ステータス管理: 下書き、完成
   - JSONフィールドでフォームデータを保存

2. **書類作成画面**
   - URL: `/clients/<client_id>/documents/create/<document_type>/`
   - テンプレート: `templates/clients/document_create.html` (428行)
   - ビュー: `clients/views.py::document_create` (520行目)
   - 機能:
     - 利用者基本情報の自動反映（アコーディオン）
     - 手動入力項目（届出区分、依頼事由、有効開始日等）
     - 履歴データの読み込み・編集機能
     - 保存機能（下書き）
     - Excel出力機能

3. **Excel出力機能**
   - openpyxlを使用したExcel生成
   - 座標設定ファイル: `static/config/excel_coordinates.json`
   - アセスメント用座標設定: `static/config/excel_coordinates_assessment.json`
   - リファレンス: `static/config/excel_coordinates_reference.md`
   - テンプレートファイル: `templates/forms/*.xlsx`

4. **Excelテンプレートファイル（templates/forms/）**
   - kyotaku_service_plan_request.xlsx（居宅サービス計画作成依頼書）
   - assessment_sheet.xlsx（アセスメントシート）
   - kyotaku_preventive_service_plan_request.xlsx（介護予防サービス計画作成依頼書）
   - care_service_meeting_notice.xlsx（担当者会議開催通知）
   - care_provider_selection_confirm.xlsx（サービス事業者選択確認書）
   - hospital_admission_info_form.xlsx（医療機関・入院時情報連携加算様式）
   - medical_care_coordination_sheet.xlsx（医療・介護連携シート）
   - Face Sheet.xlsx（フェイスシート）
   - LTC_Certification_Change_R4-08.xlsx（要介護認定・要支援認定区分変更申請書）
   - LTC_Certification_Renewal_R4-08.xlsx（要介護認定・要支援認定更新申請書）
   - LTC_Reissue_Application.xlsx（被保険者証再交付申請書）
   - CarePlan_Info_Request.xls（介護サービス計画作成依頼届出書）

5. **PDF生成機能**
   - PDFGeneratorクラス（clients/pdf_utils.py）
   - reportlabを使用
   - 日本語フォント対応（HeiseiKakuGo-W5）
   - 背景画像配置機能
   - 西暦→令和変換機能
   - 座標設定JSON対応（`static/config/pdf_coordinates.json`）
   - 背景画像: `static/images/backgrounds/kyotaku_service_plan_request-1.png`
   - **注意**: PDF出力機能のコードは実装されているが、現在のビューでは使用されていない

##### 未実装・課題
1. **PDF出力の実装**
   - PDFGeneratorクラスは存在するが、ビューから呼び出されていない
   - PDF座標設定ファイル（pdf_coordinates.json）の作成が必要

2. **書類種別の拡張**
   - 現在、居宅サービス計画作成依頼書のみがビューで対応
   - アセスメントシート等の他の書類種別の実装が必要

3. **アセスメントデータとの連携**
   - アセスメントデータから書類への自動反映機能の拡充

---

## 現在の実装状況

### 書類作成の基本機能
- [x] 書類作成フォーム画面
- [x] 利用者データの自動反映
- [x] 履歴管理（保存・編集機能）
- [x] Excel出力機能
- [ ] PDF出力機能（コードは存在するが未使用）
- [ ] アセスメントデータに基づく書類生成

### 対応している書類種類
- [x] 居宅サービス計画作成依頼（変更）届出書（Excel出力対応・事業者選択機能）
- [x] 介護予防サービス計画作成・介護予防ケアマネジメント依頼（変更）届出書（Excel出力対応・事業者選択機能）
- [ ] アセスメントシート（テンプレートあり、Excel座標設定あり、ビュー未実装）
- [ ] 担当者会議開催通知（テンプレートあり）
- [ ] サービス事業者選択確認書（テンプレートあり）
- [ ] その他の書類（テンプレート多数あり）

### 技術スタック
- **バックエンド**: Django 4.2.16
- **PDF生成**: reportlab（日本語フォント対応）
- **Excel操作**: openpyxl
- **座標管理**: JSON設定ファイル

---

## 次のステップ

### 優先度: 高
1. PDF出力機能の実装（ビューへの統合）
2. アセスメントシートの書類作成機能実装
3. 他の書類種別のビュー実装

### 優先度: 中
4. アセスメントデータとの連携強化
5. 座標設定の管理画面作成
6. 書類プレビュー機能

### 優先度: 低
7. テンプレートのカスタマイズ機能
8. 一括出力機能

---

## ファイル構成

### モデル
- `clients/models.py` - DocumentCreationHistoryモデル

### ビュー
- `clients/views.py` - document_create, generate_document_excel

### ユーティリティ
- `clients/pdf_utils.py` - PDFGenerator クラス

### テンプレート
- `templates/clients/document_create.html` - 書類作成フォーム

### 設定ファイル
- `static/config/excel_coordinates.json` - Excel座標設定
- `static/config/excel_coordinates_assessment.json` - アセスメント用Excel座標設定
- `static/config/excel_coordinates_reference.md` - 座標設定リファレンス
- `static/config/pdf_coordinates.json` - PDF座標設定（未作成）

### テンプレートファイル
- `templates/forms/*.xlsx` - 各種Excelテンプレート（12種類以上）

### 画像
- `static/images/backgrounds/kyotaku_service_plan_request-1.png` - PDF背景画像

---

## 開発メモ

### Excel座標設定の例（居宅サービス計画作成依頼書）
```json
{
  "kyotaku_service_plan_request": {
    "client_name": "A6",
    "client_furigana": "D5",
    "insurance_number": "N5",
    "birth_date": "O10",
    "effective_start_date": "T21",
    ...
  }
}
```

### 書類作成フローの確認事項
1. ユーザーが利用者詳細ページから「書類作成」を選択
2. 書類種別を選択
3. フォームに入力（利用者情報は自動反映）
4. 「保存」または「Excel出力」を選択
5. 保存の場合は履歴に保存され、利用者詳細ページに戻る
6. Excel出力の場合は、テンプレートに座標設定に基づいてデータを書き込み、ダウンロード

---

## 開発計画（2025-11-18 15:45〜）

### 開発方針
すべての書類作成機能を「居宅サービス計画作成依頼（変更）届出書」と同じレイアウト・構造で統一して開発します。

### 開発の2段階アプローチ
**フェーズ1: 入力フォームの作成**
- 各書類ごとに入力フォームを作成
- 既存の `document_create.html` をベースに統一的なレイアウトで実装
- 利用者基本情報の自動反映機能を実装

**フェーズ2: Excel座標設定**
- 各書類のExcelテンプレートに対する座標設定を作成
- `static/config/excel_coordinates.json` に追加
- Excel出力機能のテスト

### 対象書類リスト（優先順位順）

#### 1. 介護保険関係
1. ✅ 居宅サービス計画作成依頼（変更）届出書 - **完成**（事業者選択・Excel出力対応）
2. ✅ 介護予防サービス計画作成・介護予防ケアマネジメント依頼（変更）届出書 - **完成**（事業者選択・Excel出力対応）
3. ⏳ 介護保険 要介護・要支援 [認定区分変更]申請書
4. ⏳ 介護保険　要介護・要支援　（更新）認定申請書
5. ⏳ 介護保険関係・再交付申請書
6. ⏳ 居宅サービス事業所の選択に関する説明に係る確認書

#### 2. ケアプラン・サービス関係
7. ⏳ 介護サービス計画作成に係る資料提供申請書
8. ⏳ サービス担当者会議開催のご案内

#### 3. 基本情報・医療連携
9. ⏳ フェースシート
10. ⏳ 入院時情報提供書
11. ⏳ 医療・介護連携シート

### 各書類の開発タスク

各書類につき以下のタスクを実施：

**入力フォーム作成段階:**
1. [ ] Excelテンプレートの内容確認（必要な入力項目の特定）
2. [ ] DocumentCreationHistoryモデルに書類種別を追加
3. [ ] document_createビューに書類種別の対応を追加
4. [ ] 入力フォームHTMLの作成（document_create.htmlを拡張）
5. [ ] フォームの動作確認

**Excel座標設定段階:**
6. [ ] Excelテンプレートのセル座標を調査
7. [ ] excel_coordinates.jsonに座標設定を追加
8. [ ] generate_document_excel関数の対応確認
9. [ ] Excel出力のテスト
10. [ ] 完成

### 技術的な統一事項

**共通レイアウト構成:**
```
1. タイトル行（書類名）
2. 利用者名と戻るボタン
3. 利用者基本情報（アコーディオン）- 自動反映
4. 手動入力項目（カード）
5. 保存・Excel出力ボタン
```

**共通機能:**
- 利用者基本情報の自動反映
- 履歴からの編集機能
- 下書き保存機能
- Excel出力機能
- 入力バリデーション

**ファイル命名規則:**
- 書類種別キー: スネークケース（例: `care_provider_selection_confirm`）
- テンプレートファイル: `templates/forms/{書類種別キー}.xlsx`
- 座標設定: `excel_coordinates.json` 内の `{書類種別キー}` キー

### 現在の作業状況
- [x] 開発方針の策定
- [x] 書類一覧の整理
- [ ] 次の書類の入力フォーム作成開始

---

## 開発進捗記録

### 今後の記録フォーマット
各書類の開発時に以下の形式で記録：

```
#### [書類名] - [日付]
**フェーズ**: 入力フォーム作成 / Excel座標設定
**ステータス**: 作業中 / 完成
**変更ファイル**:
- リスト
**備考**:
- 特記事項
```
