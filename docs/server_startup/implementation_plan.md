# サーバー起動とサイト閲覧の計画

サーバーを起動して、ブラウザでサイトを確認できるようにします。

## 提案される変更

コードの変更は必要ありません。以下のコマンドを実行してサーバーを起動します。

### サーバー起動プロセス

1. `python manage.py runserver` を実行して開発用サーバーを起動します。
2. サーバーが正常に起動したことを確認します。

## 検証計画

### 手動検証
- ブラウザで `http://127.0.0.1:8000` にアクセスし、サイトが表示されることを確認します。
